<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.berryst.demo.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.berryst.demo.module.User" >
        <id column="user_id" property="user_id" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="email" property="email" jdbcType="VARCHAR" />
    </resultMap>
    <select id="queryUserList" resultType= "BaseResultMap">
        select * from user
    </select>

    <select id="queryUserById" parameterType="int" resultType="BaseResultMap">
        select * from user where user_id = #{user_id}
    </select>

    <select id="queryUserByEmail" parameterType="String" resultType="BaseResultMap">
        select * from user where email = #{email}
    </select>

    <insert id="addUser" parameterType="BaseResultMap">
        insert into user (username, password, email) values (#{username}, #{password}, #{email})
    </insert>

    <update id="updateUser" parameterType="BaseResultMap">
        update user set username = #{username}, password = #{password}, email = #{email} where user_id = #{user_id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where user_id = #{user_id}
    </delete>
</mapper>